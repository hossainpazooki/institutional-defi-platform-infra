apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
type: Opaque
stringData:
  # For local development only - do not use real keys here
  DATABASE_URL: "postgresql://postgres:postgres@host.minikube.internal:5432/institutional_defi"
  REDIS_URL: "redis://host.minikube.internal:6379/0"
  ANTHROPIC_API_KEY: "sk-placeholder-set-via-env"
